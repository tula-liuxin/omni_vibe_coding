# omni_vibe_coding
- codex-tmux-bundle/ 多账号 tmux + Codex 配置包（每个 session 固定一个账号）
- 新增：`codex-account-limits` 输出各账号限额
- 新增：`codex-agent-*` 层级代理工具（新建 window/pane 并启动 codex）
- 新增：`codex-run` 启动 codex 并绑定当前 session 账号
- 新增：tmux 鼠标支持（`~/.tmux.conf`：`set -g mouse on`，`setw -g mode-keys vi`）
- 使用流程已写入 `codex-tmux-bundle/README.md`（含 `/mnt/c/Users/23677/agencyarche` 示例）
